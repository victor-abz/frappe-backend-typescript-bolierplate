{
	"name": "frappe-backend-typescript",
	"version": "1.0.0",
	"description": "Boilerplate for Frappe backend with typescript",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"new-model": "frp-cmd new-model",
		"start": "npm run build && frp-cmd start",
		"build": "rimraf ./build && tsc"
	},
	"keywords": [
		"boilerplate",
		"frappe-backend",
		"frappe"
	],
	"author": "Abizeyimana Victor",
	"license": "ISC",
	"devDependencies": {
		"@types/express": "^4.17.13",
		"@types/node": "^17.0.23",
		"install": "^0.13.0",
		"npm": "^8.5.5",
		"rimraf": "^3.0.2",
		"typescript": "^4.6.3"
	},
	"dependencies": {
		"express": "^4.17.3",
		"frappe-backend": "^1.1.7"
	}
}
